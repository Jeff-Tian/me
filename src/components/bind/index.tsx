import { View } from "@tarojs/components";
import { AtButton } from "taro-ui";

export default function BindOtherSocialAccounts(props) {

    return <View>
        <AtButton type="primary">绑定微信</AtButton>
    </View>
}